;;; config.el -*- lexical-binding: t; -*-

(require 'pspiagicw-defaults)
(require 'pspiagicw-packages)
(require 'pspiagicw-use-package)
;; Loadejk
(require 'pspiagicw-ui)
(require 'pspiagicw-modeline)
;; ;; Load the dashboard
(require 'pspiagicw-dashboard)
;; ;; Completion
(require 'pspiagicw-completion)
(require 'pspiagicw-evil)
(require 'pspiagicw-bindings)
;; Load window defaults
(require 'pspiagicw-window)
;; Load all the important bindings
;; Project settings
(require 'pspiagicw-projects)
;; (require 'pspiagicw-mail)
;; Company-mode
(require 'pspiagicw-company)
(require 'pspiagicw-lsp)
(require 'pspiagicw-shell)
(require 'pspiagicw-dired)
(require 'pspiagicw-erc)

;;;; LANGUAGE Configuration ;;;;
(require 'pspiagicw-programming)
(require 'pspiagicw-python)
(require 'pspiagicw-golang)
(require 'pspiagicw-flutter)
(require 'pspiagicw-other-prog)
(require 'pspiagicw-org)
(require 'pspiagicw-misc)
